rootProject.name = 'Vampirism'

def usePublishedEpicCore = gradle.startParameter.projectProperties.get('use_published_epiccore')?.toBoolean() ?: false
def epicCoreRepoDir = file('../hytale-epiccore')
if (!usePublishedEpicCore && epicCoreRepoDir.exists()) {
    includeBuild(epicCoreRepoDir)
}
