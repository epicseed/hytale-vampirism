package com.epicseed.epiccore.modifier;

import java.util.UUID;

/**
 * Minimal identity contract for subjects participating in modifier evaluation.
 */
public interface ModifierSubject {
    UUID uuid();
}
